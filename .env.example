# Use this file as a template. Do NOT commit a real .env with secrets.
# Replace values for each environment: development, staging, production

# Application
NODE_ENV=development
PORT=3001

# MongoDB
# Use different URIs for dev/staging/production
MONGODB_URI=mongodb://localhost:27017/myapp_dev
MONGODB_POOL_SIZE=10

# JWT / Auth
JWT_SECRET=replace-with-strong-secret
JWT_EXPIRES_IN=7d

# External services
SENTRY_DSN=  # optional: https://sentry.io/

# Hosting / Deployment
# For Render: RENDER_SERVICE_ID (not in .env — set in GitHub secrets)
# For Vercel: VERCEL_TOKEN (not in .env)
# For Heroku: HEROKU_API_KEY (not in .env)

# Example extra flags
LOG_LEVEL=info